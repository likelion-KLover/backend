input {
  rabbitmq {
    host => "localhost"
    port => 5672
    queue => "ES_COMMPOST_UPDATE"
    user => "admin"
    password => "admin"
    codec => json
  }
}
output {
    elasticsearch {

    }
}