server:
  port: 8080
  timezone: Asia/Seoul
spring:
  messages:
    basename: locales/messages
    fallback-to-system-locale: false
    always-use-message-format: true
    encoding: UTF-8
  application:
    name: my-application
  profiles:
    active: dev
    include: secret
aws:
  bucket_name: klover-bucket-jun2-1
